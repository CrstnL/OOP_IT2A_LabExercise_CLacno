public class Exer2_Declarative {
    public static void main(String[] args) {
        // Declarative: directly state the formula for area
        
        int length = 5;
        int width = 3;
        int area = (length * width);

        System.out.println("Area of rectangle = " + area);
    }
}